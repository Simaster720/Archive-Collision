// Landing = collection cover. Phase 4 builds the full CollectionCover
// (minimal title-centric cover). The persistent sidebar lives in the layout.
export default function Home() {
  return (
    <section className="flex min-h-[60vh] flex-col items-center justify-center p-10 text-center md:min-h-screen">
      <h1 className="text-2xl font-semibold tracking-tight md:text-3xl">
        신수찬 컬렉션
      </h1>
    </section>
  );
}
